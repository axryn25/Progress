# A user has only 3 attempts to enter the correct password ("python123"). 
# Using a while loop, allow repeated attempts and display whether they logged in successfully or are blocked after 3 failures.
i = 0 #initialize the count

while i < 3:
    a = input("enter password: ") #user unput taken
    if a == "python123":
        print("logged in successfully!!!")
        break
    else:
        print("Wrong password. Attempts left: ",2-i) #iteration starts from 0 hence we use 2-i instead of 3-i
        i += 1
         

if i == 3: #so when count hits 3 , it displays this message
    print("you can't enter no more")


